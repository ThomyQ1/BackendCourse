
Resumen del Código: Lo que hice fue, crear una clase llamada UserManager que se encarga de gestionar usuarios. La clase utiliza el módulo fs para realizar operaciones de sistema de archivos, tales como leer, escribir en archivos y manejar rutas de archivos.
En el constructor de la clase, se configuran las rutas para la carpeta de datos y el archivo de usuarios. También se crea la carpeta de datos si aún no existe y se intenta leer la lista de usuarios desde el archivo.
Para luego, hacer que se instancie la clase UserManager y se ejecutan algunas operaciones de forma asíncrona para demostrar su funcionamiento.

Ahora si nos referimos a ProductManager, lo que hice fue crear una clase llamada ProductManager que se encarga de gestionar productos almacenados en un archivo JSON, de la misma manera realizada con UserManager. La clase utiliza los módulos fs y path para operaciones de sistema de archivos y manejo de rutas.
En el constructor de la clase, se establecen las rutas para la carpeta de datos y el archivo de productos. Además, se crea la carpeta de datos si no existe y se intenta leer la lista de productos desde el archivo.
Finalmente, se instancia la clase ProductManager y se ejecutan algunas operaciones de forma asíncrona para demostrar su funcionamiento, como leer, crear productos y mostrar toda la informacion de un producto.